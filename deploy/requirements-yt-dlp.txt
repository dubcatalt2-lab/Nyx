# Server-side tool only. Nyx passes only validated 11-character YouTube video IDs for bounded public-transcript discovery and optional native playback; arbitrary client URLs are never accepted.
yt-dlp[default]==2026.08.19
