import assert from 'node:assert/strict';import {createMovieCatalog} from '../lib/movies.mjs';
const series={id:95479,name:'Jujutsu Kaisen',media_type:'tv',first_air_date:'2020-10-03',poster_path:'/abc.jpg',genres:[],credits:{cast:[]},seasons:[{season_number:1,name:'Season 1',episode_count:1}]};
const c=createMovieCatalog({token:()=> 'test',fetchImpl:async url=>{if(url.pathname.endsWith('/search/multi'))return Response.json({results:[series,{id:4,media_type:'person',name:'Actor'},{id:5,media_type:'movie',title:'Film',adult:true}],total_pages:1});if(url.pathname.endsWith('/season/1'))return Response.json({season_number:1,episodes:[{season_number:1,episode_number:1,name:'First episode'}]});assert.equal(url.pathname,'/3/tv/95479');return Response.json(series);}});
const results=await c.search('Jujutsu Kaisen');assert.equal(results.results.length,1);assert.equal(results.results[0].kind,'tv');assert.equal(results.results[0].title,series.name);assert.equal(results.results[0].poster,'https://image.tmdb.org/t/p/w342/abc.jpg');
const detail=await c.series(95479);assert.equal(detail.kind,'tv');assert.equal(detail.episodes[0].id,'jujutsu-kaisen-1');await assert.rejects(()=>c.series('../bad'));
assert.equal((await c.episode(95479,1,1)).sources[0].url,'https://nhdapi.com/tv/95479/1/1');await assert.rejects(()=>c.episode(95479,1,99),e=>e.status===404);await assert.rejects(()=>c.season(95479,999),e=>e.status===404);await assert.rejects(()=>c.season(95479,-1),e=>e.status===400);
console.log('PASS: combined movie/TV search, series title/date/artwork normalization, person/adult filtering and correct episode mapping.');
